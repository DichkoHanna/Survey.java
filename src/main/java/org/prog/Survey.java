package org.prog;

public class Survey {
    private String[] myStudents = new String[5];

    public int takeStudent (int index) {
        this.myStudents = myStudents;
        if (index < myStudents.length && myStudents [studentMark] = 0);{
            System.out.println(  + " answer the question, please");
            myStudents [studentsName] = name;
                  }
            return index;
    }

}
